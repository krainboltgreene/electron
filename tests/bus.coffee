chai   = require "chai"
sinon  = require "sinon"
should = chai.should()

chai.use(require "sinon-chai")

Bus = require "../lib/bus.coffee"

describe "Bus", ->

    describe "#constructor()", ->

    describe "#addSignal()", ->

    describe "#all()", ->

    describe "#merge()", ->
