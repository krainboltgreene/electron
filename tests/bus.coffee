chai   = require "chai"
should = chai.should()

chai.use(require "sinon-chai")

describe "Bus", ->
    describe "#constructor()", ->
    describe "#addSignal()", ->
    describe "#all()", ->
    describe "#merge()", ->
