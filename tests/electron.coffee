chai   = require "chai"
should = chai.should()

chai.use(require "sinon-chai")

describe "Electron", ->
    describe "#fromPromise()", ->
    describe "#fromEventTarget()", ->
    describe "#asEventStream()", ->
